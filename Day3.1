## Maximum Subarry Sum

#include <bits/stdc++.h> 
long long maxSubarraySum(int arr[], int n)
{
    int ans=0;
    int MAX = INT_MIN;
    for(int i=0; i<n; i++){
        ans += arr[i];
        MAX = max(ans, MAX);
        
    }
    
   
    return MAX;
}
