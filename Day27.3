## Minimum Number of Plateforms    TC : O(2nlogn) + O(2n)   SC : O(1)

int calculateMinPatforms(int at[], int dt[], int n) {
    // Write your code here.
    sort(at, at+n);
    sort(dt, dt+n);
    
    int plateform_needed = 1, ans = 1;
    
    int i=1, j=0;
    while(i<n && j<n){
        if(at[i] <= dt[j]){
            plateform_needed++;
            i++;
        }
        else if (at[i] >= dt[j]){
            plateform_needed--;
            j++;
        }
        if(plateform_needed > ans){
            ans = plateform_needed;
        }
    }
    return ans;
}
