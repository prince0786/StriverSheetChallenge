## Reverse Word In A String

string reverseString(string str)
{
    // Write your code here.
    string temp = "";
    int n = str.length();
    int i=n-1;
    while(i>=0){
        while(i>=0 && str[i]==' ')
            i--;
        int j=i;
        while(j>=0 && str[j]!=' ')
            j--;
        temp += (str.substr(j+1,i-j));
        temp += ' ';
        i=j;
    }
    return temp;
}
