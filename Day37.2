## Single Element in a Sorted Array

#include<bits/stdc++.h>
int uniqueElement(vector<int> arr, int n)
{
	// Write your code here ***1. hashing Approach
    /*unordered_map<int, int>mp;
    for(int i=0; i<n; i++){
        mp[arr[i]]++;
    }
    for(int it = 0; it<n; it++){
        int key = arr[it];
        auto temp = mp.find(key);
        if(temp->second == 1)
            return key;
    }
    return 0;  */
    // TC : O(N) SC : O(1)
//2ND Approach
    int xorr = 0;   
    for(int i=0; i<n; i++){
        xorr = xorr ^ arr[i];
    }
    return xorr;
}
